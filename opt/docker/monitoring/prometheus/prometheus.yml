global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["prometheus:PRM_PORT"]

  - job_name: "node"
    static_configs:
      - targets: ["node-exporter:NODE_EXP_PORT"]